import { LookupCodePipe } from './lookup-code.pipe';

describe('LookupCodePipe', () => {
  it('create an instance', () => {
    const pipe = new LookupCodePipe();
    expect(pipe).toBeTruthy();
  });
});
