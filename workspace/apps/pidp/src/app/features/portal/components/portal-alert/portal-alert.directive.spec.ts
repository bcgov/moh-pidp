import { PortalAlertDirective } from './portal-alert.directive';

describe('PortalAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new PortalAlertDirective();
    expect(directive).toBeTruthy();
  });
});
