import { BreadcrumbButtonDirective } from './breadcrumb-button.directive';

describe('BreadcrumbButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BreadcrumbButtonDirective();
    expect(directive).toBeTruthy();
  });
});
